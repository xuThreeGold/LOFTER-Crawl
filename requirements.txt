# Python版本要求
# Python >= 3.7, 推荐 Python 3.8-3.12

# 核心依赖包（必需）
requests>=2.27.1          # HTTP请求库
lxml>=5.0.0               # XML/HTML解析库（使用新版本以支持Python 3.12预编译wheel）
urllib3>=1.26.9           # URL处理库（requests的依赖）
html2text>=2020.1.16      # HTML转文本库

# 注意：
# 1. 如果使用 Python 3.12，lxml>=5.0.0 通常有预编译wheel，无需编译
# 2. 如果仍遇到编译问题，可以：
#    - 安装 Microsoft C++ Build Tools: https://visualstudio.microsoft.com/visual-cpp-build-tools/
#    - 或使用 conda 安装: conda install lxml
#    - 或降级到 Python 3.10 或 3.11

# 以下包为标准库，无需安装
# - re (正则表达式)
# - time (时间处理)
# - random (随机数)
# - os (操作系统接口)
# - urllib.parse (URL解析)
